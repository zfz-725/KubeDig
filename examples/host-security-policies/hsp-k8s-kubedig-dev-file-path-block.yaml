apiVersion: security.kubedig.com/v1
kind: KubeDigHostPolicy
metadata:
  name: hsp-kubedig-dev-file-path-block
spec:
  nodeSelector:
    matchLabels:
      kubernetes.io/hostname: kubedig-dev
  severity: 5
  file:
    matchPaths:
    - path: /etc/hostname
  action:
    Block

# kubedig-dev_test_03

# test
# $ cat /etc/hostname
# cat: /etc/hostname: Permission denied

# expectation
# anyone cannot access /etc/hostname
