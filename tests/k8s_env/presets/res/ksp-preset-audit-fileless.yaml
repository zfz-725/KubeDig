apiVersion: security.kubedig.com/v1
kind: KubeDigPolicy
metadata:
  name: ksp-preset-audit-fileless
  namespace: presets
spec:
  action: Audit
  presets:
  - filelessExec
  selector:
    matchLabels:
      app: fileless
  severity: 8