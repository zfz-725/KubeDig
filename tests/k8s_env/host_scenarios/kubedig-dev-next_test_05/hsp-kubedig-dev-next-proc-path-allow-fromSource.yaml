apiVersion: security.kubedig.com/v1
kind: KubeDigHostPolicy
metadata:
  name: hsp-kubedig-dev-next-proc-path-allow-fromsource
spec:
  nodeSelector:
    matchLabels:
      kubernetes.io/hostname: kubedig-dev-next
  severity: 5
  process:
    matchPaths:
    - path: /bin/date
      fromSource:
      - path: /bin/bash # ubuntu
    - path: /usr/bin/date
      fromSource:
      - path: /usr/bin/bash # centos
  action:
    Allow

# kubedig-dev-next_test_05

# test
# $ bash -c date
# ...
# $ bash -c ls
# bash: /usr/bin/ls: Permission denied

# expectation
# (/usr)/bin/bash can only execute (/usr)/bin/date
# (/usr)/bin/bash cannot execute any others
