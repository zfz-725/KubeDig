apiVersion: security.kubedig.com/v1
kind: KubeDigPolicy
metadata:
  name: malformated-array-ls-block
  namespace: multicontainer
spec:
  severity: 4
  selector:
    matchLabels:
      container: multicontainer
      kubedig.io/container.name: "[container-1,,]"
  process:
    matchPaths:
    - path: /bin/ls
      # ls
  action:
    Block
