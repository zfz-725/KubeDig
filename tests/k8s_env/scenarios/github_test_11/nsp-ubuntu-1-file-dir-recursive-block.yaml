apiVersion: security.kubedig.com/v1
kind: KubeDigPolicy
metadata:
  name: nsp-ubuntu-1-file-dir-recursive-block
  namespace: github
spec:
  selector:
    matchLabels:
      container: ubuntu-1
  apparmor: |
    deny /etc/{*,**} rw,
