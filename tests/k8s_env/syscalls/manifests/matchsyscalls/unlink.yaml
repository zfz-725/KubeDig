apiVersion: security.kubedig.com/v1
kind: KubeDigPolicy
metadata:
  name: audit-all-unlink
  namespace: syscalls
spec:
  severity: 3
  selector:
    matchLabels:
      container: ubuntu-1
  syscalls:
    matchSyscalls:
    - syscall:
      - unlink
  action:
    Audit
